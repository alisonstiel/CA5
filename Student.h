#pragma once
#include <stdlib.h>
#include <unordered_map>
#include <fstream>
class Student{
	private:
		std::unordered_map student; //student's schedule
		std::unordered_map courses; //possible courses
	public:
		
};
